services:
  discord-bot:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: gmc-bot
    restart: unless-stopped
    init: true
    env_file:
      - .env
    environment:
      - DISCORD_API_TOKEN=${API_TOKEN}


